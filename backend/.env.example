# Copy to .env and adjust. Never commit the real .env.
# Local dev defaults to SQLite if DATABASE_URL is unset.
DATABASE_URL=sqlite:///./hospital.db
JWT_SECRET=change-me-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=60
SEED_DEMO_DATA=true
